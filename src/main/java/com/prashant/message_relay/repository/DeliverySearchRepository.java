package com.prashant.message_relay.repository;

/**
 * Elasticsearch removed: keep a placeholder interface to avoid compile issues
 * in places that may still reference the symbol. The application no longer
 * performs Elasticsearch-backed searches.
 */
public interface DeliverySearchRepository {

}
